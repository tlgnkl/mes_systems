# 🚀 Быстрый старт - FastAPI + React CRUD с ChatGPT

## ⚠️ Версия Python

**Используйте Python 3.11.10** (уже установлена в проекте)

❌ **НЕ используйте 3.13** - могут быть проблемы совместимости с некоторыми пакетами

Проверьте версию:
```bash
python --version
```

Должно быть: `Python 3.11.10`

---

## 📋 Предварительные требования

- ✅ Python 3.11.10
- ✅ Node.js 18+ (npm)
- ✅ Git

Проверьте установку:
```bash
python --version    # Python 3.11.10
node --version      # v18+
npm --version       # 9+
```

---

## 🔧 Установка и запуск

### Шаг 1: Откройте два терминала

**Терминал 1** - для Backend
**Терминал 2** - для Frontend

---

### Шаг 2: Backend (Терминал 1)

```bash
# Перейдите в папку backend
cd /Users/tlgn/CascadeProjects/mes_systems/15nov/my_fastapi_project/backend

# Убедитесь, что используется Python 3.11.10
python --version

# Создайте виртуальное окружение (если его нет)
python -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Запустите сервер
uvicorn main:app --reload --port 8000
```

**Ожидаемый результат:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete
```

✅ Backend готов на: http://localhost:8000

---

### Шаг 3: Frontend (Терминал 2)

```bash
# Перейдите в папку frontend
cd /Users/tlgn/CascadeProjects/mes_systems/15nov/my_fastapi_project/frontend

# Установите зависимости (если их нет)
npm install

# Запустите dev сервер
npm run dev
```

**Ожидаемый результат:**
```
➜  Local:   http://localhost:3000/
```

✅ Frontend готов на: http://localhost:3000

---

## 🌐 Откройте приложение

1. Откройте браузер
2. Перейдите на: **http://localhost:3000**

Вы должны увидеть:
- Навигацию с логотипом "ItemManager"
- Две вкладки: "Items" и "AI Chat"
- Список товаров (пока пустой)

---

## 📝 Первые шаги

### 1. Создайте товар

1. Нажмите кнопку **"Add Item"**
2. Заполните форму:
   - **Title**: "Ноутбук"
   - **Description**: "Мощный ноутбук для работы"
   - **Price**: "50000"
3. Нажмите **"Create Item"**

### 2. Используйте AI функции

- 🪄 **Кнопка волшебной палочки** - генерирует заголовок из описания
- ✨ **"Improve with AI"** - улучшает описание
- 🧠 **Кнопка мозга** - полный AI анализ товара

### 3. Общайтесь с AI

1. Нажмите вкладку **"AI Chat"**
2. Напишите сообщение: "Помоги описать ноутбук"
3. Получите ответ от ChatGPT

---

## 🔗 Полезные ссылки

| Ссылка | Описание |
|--------|---------|
| http://localhost:3000 | Главное приложение |
| http://localhost:8000 | Backend API |
| http://localhost:8000/docs | API документация (Swagger) |
| http://localhost:8000/redoc | API документация (ReDoc) |

---

## ⚙️ Конфигурация

### Backend (.env)

Файл: `backend/.env`

```env
# OpenAI API Settings
OPENAI_API_KEY=sk-proj-...  # Ваш ключ OpenAI
OPENAI_MODEL=gpt-4o-mini    # Модель ChatGPT

# App settings
DEBUG=true
```

**Как получить OPENAI_API_KEY:**
1. Перейдите на https://platform.openai.com/api-keys
2. Создайте новый ключ
3. Скопируйте его в `.env`

### Frontend

API URL автоматически настроен на: `http://localhost:8000`

---

## 🐛 Решение проблем

### ❌ Backend не запускается

**Проблема:** `Address already in use`

**Решение:**
```bash
# Найдите процесс на порту 8000
lsof -ti:8000

# Завершите процесс
kill -9 <PID>

# Или используйте другой порт
uvicorn main:app --reload --port 8001
```

### ❌ Frontend не запускается

**Проблема:** `Port 3000 is already in use`

**Решение:**
```bash
# Используйте другой порт
PORT=3001 npm run dev
```

### ❌ Ошибка CORS

**Проблема:** `Access to XMLHttpRequest blocked by CORS policy`

**Решение:** Убедитесь, что backend запущен на http://localhost:8000

### ❌ AI функции не работают

**Проблема:** `ChatGPT service is not configured`

**Решение:**
1. Проверьте наличие файла `backend/.env`
2. Убедитесь, что `OPENAI_API_KEY` заполнен
3. Проверьте статус: http://localhost:8000/chatgpt/health

### ❌ Ошибка `ModuleNotFoundError`

**Проблема:** `No module named 'fastapi'`

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements.txt
```

---

## 📊 Проверка статуса

### Backend здоров?

```bash
curl http://localhost:8000/health
```

Ожидаемый ответ:
```json
{
  "status": "healthy",
  "version": "1.0.0",
  "services": {
    "database": "available",
    "chatgpt": "available"
  }
}
```

### Frontend работает?

Откройте http://localhost:3000 в браузере

---

## 🛑 Остановка приложения

### Остановить Backend
В терминале 1: нажмите **Ctrl+C**

### Остановить Frontend
В терминале 2: нажмите **Ctrl+C**

---

## 📚 Дополнительно

### Структура проекта

```
my_fastapi_project/
├── backend/
│   ├── main.py              # Точка входа
│   ├── routers/             # API роутеры
│   ├── services/            # Сервисы (ChatGPT)
│   ├── models.py            # БД модели
│   ├── schemas.py           # Pydantic схемы
│   ├── crud.py              # CRUD операции
│   ├── database.py          # БД конфигурация
│   ├── requirements.txt      # Зависимости Python
│   └── .env                 # Конфигурация
│
└── frontend/
    ├── src/
    │   ├── components/      # React компоненты
    │   ├── pages/          # Страницы
    │   ├── hooks/          # Кастомные хуки
    │   ├── services/       # API клиент
    │   └── styles/         # CSS/Tailwind
    ├── package.json        # Зависимости Node
    └── vite.config.js      # Конфигурация Vite
```

### Основные команды

**Backend:**
```bash
# Запуск в режиме разработки
uvicorn main:app --reload

# Запуск в production
uvicorn main:app --host 0.0.0.0 --port 8000

# Запуск с другим портом
uvicorn main:app --reload --port 8001
```

**Frontend:**
```bash
# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Preview production build
npm run preview
```

---

## ✅ Чек-лист запуска

- [ ] Python 3.11.10 установлен
- [ ] Node.js 18+ установлен
- [ ] Виртуальное окружение создано
- [ ] Backend зависимости установлены (`pip install -r requirements.txt`)
- [ ] Frontend зависимости установлены (`npm install`)
- [ ] Backend запущен на http://localhost:8000
- [ ] Frontend запущен на http://localhost:3000
- [ ] Браузер открыт на http://localhost:3000
- [ ] Видна навигация и список товаров
- [ ] Можно создать товар
- [ ] AI функции работают

---

## 🎉 Готово!

Приложение полностью настроено и готово к использованию!

**Вопросы?** Проверьте логи в терминалах или откройте http://localhost:8000/docs для API документации.
