# FastAPI + React CRUD с AI интеграцией

Полнофункциональное приложение для управления товарами с интеграцией ChatGPT.

## Структура проекта

```
my_fastapi_project/
├── backend/          # FastAPI сервер
│   ├── main.py      # Точка входа
│   ├── routers/     # API роутеры
│   ├── services/    # Сервисы (ChatGPT)
│   └── .env         # Конфигурация
└── frontend/         # React приложение
    ├── src/
    │   ├── components/  # UI компоненты
    │   ├── pages/      # Страницы
    │   ├── hooks/      # Кастомные хуки
    │   └── services/   # API клиент
    └── package.json
```

## Возможности

### CRUD операции
- ✅ Создание товаров
- ✅ Просмотр списка товаров
- ✅ Редактирование товаров
- ✅ Удаление товаров
- ✅ Поиск по названию

### AI функции
- 🤖 Генерация заголовков с помощью ChatGPT
- 🤖 Улучшение описаний товаров
- 🤖 Анализ контента товаров
- 🤖 Чат с AI ассистентом

## Запуск приложения

### 1. Запуск Backend

```bash
cd backend

# Активируйте виртуальное окружение (если нужно)
# python -m venv venv
# source venv/bin/activate  # для Mac/Linux
# venv\Scripts\activate     # для Windows

# Установите зависимости (если нужно)
# pip install -r requirements.txt

# Запуск сервера
uvicorn main:app --reload --port 8000
```

Backend будет доступен на: http://localhost:8000
- API документация: http://localhost:8000/docs

### 2. Запуск Frontend

В новом терминале:

```bash
cd frontend

# Установите зависимости (если нужно)
# npm install

# Запуск dev сервера
npm run dev
```

Frontend будет доступен на: http://localhost:3000

## Использование

### Главная страница (Items Management)
1. Нажмите "Add Item" для создания товара
2. Заполните форму:
   - **Title** - название товара
   - **Description** - описание (можно улучшить с AI)
   - **Price** - цена
3. Используйте кнопку 🪄 для генерации заголовка AI
4. Используйте "Improve with AI" для улучшения описания

### Действия с товарами
- 🧠 **Analyze** - AI анализ товара
- ✏️ **Edit** - редактирование
- 🗑️ **Delete** - удаление
- 🔍 **Search** - поиск по названию

### AI Chat
Перейдите на страницу "AI Chat" для общения с ассистентом

## Технологии

### Frontend
- **React 18** - UI библиотека
- **React Router** - маршрутизация
- **Axios** - HTTP клиент
- **Tailwind CSS** - стилизация
- **Lucide React** - иконки
- **Vite** - сборщик

### Backend
- **FastAPI** - веб-фреймворк
- **SQLAlchemy** - ORM
- **OpenAI API** - интеграция ChatGPT
- **SQLite** - база данных

## Конфигурация

### Backend (.env)
```env
OPENAI_API_KEY=your_key_here
OPENAI_MODEL=gpt-4o-mini
DEBUG=true
```

### Frontend
API URL настроен в `src/services/api.js`:
```javascript
const API_BASE_URL = 'http://localhost:8000';
```

## API Endpoints

### Items
- `GET /items/` - получить все товары
- `POST /items/` - создать товар
- `GET /items/{id}` - получить товар
- `PUT /items/{id}` - обновить товар
- `DELETE /items/{id}` - удалить товар
- `POST /items/{id}/analyze` - AI анализ

### ChatGPT
- `POST /chatgpt/chat` - общий чат
- `POST /chatgpt/analyze-item` - анализ описания
- `POST /chatgpt/generate-titles` - генерация заголовков
- `GET /chatgpt/health` - статус сервиса

## Устранение проблем

### Backend не запускается
- Проверьте, что порт 8000 свободен
- Убедитесь, что все зависимости установлены
- Проверьте наличие .env файла

### Frontend не запускается
- Проверьте, что node_modules установлены: `npm install`
- Проверьте порт 3000
- Убедитесь, что backend запущен

### AI функции не работают
- Проверьте OPENAI_API_KEY в .env
- Убедитесь, что ключ валиден
- Проверьте статус: http://localhost:8000/chatgpt/health

### CORS ошибки
- Проверьте, что frontend порт указан в backend/main.py
- По умолчанию разрешены: 3000, 5173, 5174

## Разработка

### Добавление новых компонентов
```bash
frontend/src/components/YourComponent.jsx
```

### Добавление новых роутов
1. Создайте компонент страницы в `frontend/src/pages/`
2. Добавьте роут в `App.jsx`

### Стилизация
Используйте Tailwind CSS классы:
```jsx
<div className="bg-white shadow-md rounded-lg p-4">
  Content
</div>
```

## Производство

### Build Frontend
```bash
cd frontend
npm run build
```

Результат будет в `frontend/dist/`

### Deploy Backend
```bash
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000
```

---

**Примечание**: Для production используйте:
- Environment variables для конфигурации
- PostgreSQL вместо SQLite
- Nginx для проксирования
- PM2 или Supervisor для управления процессами
